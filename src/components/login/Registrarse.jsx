import { useForm } from "react-hook-form"



const Registrarse = () => {
  
  const {
    register,
    handleSubmit,
    reset,
    formState:{errors}
  } = useForm()
  
    return (
    <div>
        


    </div>
  )
}

export default Registrarse
